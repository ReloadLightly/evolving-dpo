# Core (Phases 0-2). Torch: on Colab it's preinstalled; locally for CPU-only
# use:  pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
transformers>=4.44
datasets>=2.20
peft>=0.11
accelerate>=0.30

# Data (peer-review preference pairs; see HANDOFF.md). Only needed to run
# scripts/fetch_openreview.py — parsing a cache and the tests work without it.
openreview-py>=2.4.2

# Dev
pytest>=8.0

# Phase 3 (evolution) - install when we arm shinka_task/:
# shinka-evolve
